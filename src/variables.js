const variables = () => {
  const variables = {
    env: {
      api_url: "https://interview-api.ternakuang.id/",
    },
    colors: {
      white: "#fff",
      black: "#000",
    },
  };

  return variables;
};

export default variables;
